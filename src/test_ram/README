test for i8048 board.

проверяется:

 - часть внутренней памяти, не используемая для хранения переменных
 - внешняя память (256 байт), с адреса 0x0800 (0x0800..0x08FF).

используется: печать байтов со скоростью 9600 из бута.

